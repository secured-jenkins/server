{
  "name": "tc-server",
  "version": "1.0.0",
  "description": "This is a server that passes requests from POS based to Trust commerce gateway, then returns the response with the header ACEO: * to allow delivery to client side.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hasan Alrimawi",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
